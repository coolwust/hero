h2 Hero List
ul(class='heroes')
  li(
    *ngFor='let hero of heroes',
    (click)='onSelect(hero)',
    [class.selected]='hero === selectedHero'
  )
    span(class='badge') {{hero.id}}
    | {{hero.name}}
app-hero-editor([hero]='selectedHero')
